import DocumentManager from '../../components/documents/DocumentManager'

export default function ConsultorDocumentos() {
  return <DocumentManager title="Documentos" subtitle="Revisión documental por cláusula: consulta, descarga y firma. El rol consultor es de solo lectura (no sube ni elimina)" />
}
